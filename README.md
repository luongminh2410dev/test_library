## How to make the project running
1. Clone this project
2. Install **node_modules** with `npm install` or `yarn`
3. Build with
    - IOS:
    ```
    npm run ios
    ```
    - Android:
    ```
    npm run android
    ```