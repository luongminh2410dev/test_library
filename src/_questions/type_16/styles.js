import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    mathquill_row: {
        width: '100%',
        flexDirection: 'row',
        flexWrap: 'wrap',
        alignItems: 'center',
    },
    mathquill_text: {
        fontSize: 15,
        textAlign: 'center',
        marginRight: 4,
    },
})

export default styles;